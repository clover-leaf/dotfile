# ~/.tmuxinator/astro.yml

name: astro
root: ~/devlog/myhome/
pre_window: nvm use 20.14.0
windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
  - server:
      layout: main-vertical
      panes:
        - npm run dev

